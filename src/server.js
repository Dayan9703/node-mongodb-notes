const express = require('express');

//Initialization
const app = express();

//Settings
app.set('port', process.env.PORT || 4000);

//Middleware

// Global variables

// Routes

// Static files


module.exports = app;